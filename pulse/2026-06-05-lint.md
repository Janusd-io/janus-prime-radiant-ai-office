---
type: pulse
title: "2026-06-05 lint — first normal mechanical lint post-v0.14; the jehad-altoutou nested-corpus finding"
slug: 2026-06-05-lint
created: 2026-06-05
updated: 2026-06-05
departments: [ai-office]
confidence: high
related: [2026-06-02-verification-lint, 2026-06-02-lint, per-instance-curator-role, lint-2026-06-05-jehad-altoutou-nested-corpus]
---

# 2026-06-05 lint — first normal mechanical lint post-v0.14

On-demand lint (Michael requested; full autonomy granted). The 2026-06-02 verification-lint's carry-forward intended "a *normal mechanical lint*, not a schema-bump-driven one" — this is it. Ran against CLAUDE.md **v0.15** (per-instance-curator role formalised this morning).

**Scope / method.** 582 core AIO wiki pages scanned (whole repo minus `.git`, `sources/`, `inbox/`, the two `marketing-handoff*` bundles, and the `people/jehad-altoutou/` nested corpus — see Headline). Mechanical checks (broken refs, frontmatter, escalation aging, attribution, orphans) run deterministically via grep/awk against a 1,987-slug valid-target index. **Coverage honesty:** checks 1 (contradictions) and 2 (stale claims) were done as a *targeted* pass over the recently-active cluster, not an exhaustive cross-vault semantic scan — see those sections. Everything else is full-coverage.

## Headline

**The vault grew from ~600 to ~1,700 tracked `.md` files since 2026-06-02 — and ~1,116 of those are a single nested corpus.** `people/jehad-altoutou/` is not a person page; it is a **complete Prime Radiant instance** (its own `CLAUDE.md`, `.config.yaml`, `self.md` maintained by `/janus-brain`, plus `sources/` [1,096 files], `meetings/`, `projects/`, `entities/`, `questions/`) committed into the AIO repo. This is the dominant structural anomaly and the one genuinely high-stakes finding. It distorts every vault-wide metric, so all counts below **exclude** it. Escalated for a curator+Jehad decision (extract to its own instance repo / gitignore / keep-and-formally-exclude) at [[lint-2026-06-05-jehad-altoutou-nested-corpus]]. **Not touched** — it's someone else's work and reversible only by them.

Aside from that, the core AIO wiki is in good mechanical health — the v0.14 bump's promise (fewer methodology defects, more content-level findings) held.

## Prior carry-forward queue — execution status

| # | Item | Status |
|---|---|---|
| 1 | Process 2 inbox Google items | ✅ done (2026-06-03 GEO ingest) |
| 2 | 8 `entities/vendors/` stragglers | ⏳ still 8 (attio, caddy, cloudflare, cookiebot, cosmic, factset, onecli-agent-vault, vercel) — accepted-but-deprecated; curation |
| 3 | 6 `entities/people/` stragglers | ⏳ now **7** (added emma-mccall) — curation |
| 4 | 2 `.tmp` files in `vendors/` | ✅ **removed this lint** (`rm vendors/*.tmp` — ran as Claude Code in-repo, no sandbox mount issue) |
| 5 | Lint-script §5.3-step-8 update | n/a — no code-level lint runner exists; `scripts/` holds only `build-daily-snapshot.py` + a git-lock-cleanup launchd plist |
| 6 | Dimon duplicate | ⏳ curator-decision-blocked |
| 7 | Marketing-handoff slug resolution | ⏳ curator-decision-blocked; a **second** handoff bundle (`marketing-handoff-2026-05-22-broader-marketing-corpus/`, 67 files) is also present |
| 8 | Fireflies-blocked meeting source | ⏳ external blocker (= the one aging escalation, below) |
| 9 | Out-of-band 2026-06-03 timestamp | ℹ️ noted; assume workstation clock-skew unless Jehad reports otherwise |

## Check-by-check

### 1. Contradictions — *targeted pass*
No contradictions found in the recently-active cluster (Anthropic/Claude/Google vendor pages; recursive-self-improving-loop / knowledge-compilation / post-rag concepts; June pulse entries). The 2026-06-04 ingests are internally consistent with the pages they updated. **Not exhaustively scanned cross-vault** — flagged honestly; a full contradiction sweep over 582 pages is a larger pass than an on-demand lint warrants. No carry-forward item raised (no signal of a contradiction hotspot).

### 2. Stale claims — *targeted pass*
60 vendor pages carry `updated:` dates in the Feb–May window; these are AIR-registry mirrors and go stale by nature (the registry in Linear is authoritative). No specific stale-claim corrections raised this pass. The systemic item is the vendor-page schema divergence (check 6), not individual rot.

### 3. Orphans — **43**
Down to a clean number. Breakdown: 26 vendors, 9 decisions, 2 presentations, 1 project, 1 process, plus exempt non-pages (log/index). The 26 vendor orphans are mostly **AIR-registry catalog entries** (draw-io, kimi, minimax, harvey-ai, jace-ai, …) — low cross-link is expected for a registry mirror; not action-worthy. **Two real junk pages** surfaced as orphans and are cleanup candidates:
- `vendors/attio-duplicate-air-76.md` — self-titled "Attio (duplicate AIR-76)", status Backlog. A genuine duplicate of `vendors/attio.md`; merge/delete (high-stakes → curation).
- `vendors/import-your-data-linear-placeholder.md` — a Linear-import placeholder, not a real vendor. Delete candidate (curation).

### 4. Missing pages (referenced ≥2×, no page)
Derived from broken-wikilink frequency (excluding historical lint reports / index / log). Candidates:
- `2026-05-08-jehad-michael-roza-simon-euclid-bonaventure-meeting` (5×) — missing meeting source.
- `janus-prime-radiant-template` (3×), `iso-officer-process-documentation` (3×), `2026-04-30-aio-ito-compliance` (3×), `2026-05-20-forbes-viktor-series-a-75m` (2×), `2026-05-15-singapore-strategy-alignment` (2×, in `related:`).
- **Stale-slug, not missing** (targets exist under a renamed slug): `llm-wiki-build` → likely [[janus-prime-radiant-build]]; `janus-html-deck` → [[janus-html-deck-brand-guideline]]. Left un-rewritten (need confirmation they're the intended targets — not safe to guess-rewrite across multiple files).
Creating pages is high-stakes; none created this pass. Listed for curator triage.

### 5. Broken references
- **Fixed inline (2, unambiguous):** `claude-md` → `claude-md-rulebook` (in `2026-05-21-lint-followup-attribution-sweep`); reversed date-slug `transformers-are-graph-neural-networks-2026-05-13` → `2026-05-13-transformers-are-graph-neural-networks` (in `2026-05-13-magma-multi-graph-agentic-memory`).
- **Not actionable:** 19 `[[unknown-speaker-N]]` "broken links" exist **only inside historical lint reports + index/log** that *describe* the placeholder issue — not real content links. Left as-is.
- **Memory-system refs** (`feedback-*`, and `feedback_git_via_claude_code` with underscores): accepted-as-broken-by-design per §6. Minor note: one uses `feedback_` (underscore) rather than the `feedback-` convention — harmless.
- **Remaining genuine broken refs** map onto the check-4 missing-page list above.

### 6. Frontmatter compliance
- **60 vendor pages missing `confidence:`** — all in `vendors/`. This is the **AIR-import schema divergence**: registry-mirrored vendor pages use `air_id` / `status: Production` / `sensitivity` / `audience`, not the `confidence:` that §4 requires for `type: vendor`. This is systemic, not per-page rot. **Recommend a CLAUDE.md §4 clarification**: either exempt AIR-mirrored vendor pages from `confidence:` (they inherit Linear's status field) or auto-default them. Flagged for curator (potential v0.16 micro-item).
- **Serious gaps: only 2 real pages.** `assets/janus-html-deck/SKILL.md` (no frontmatter — but it's a *snapshot of an external skill bundle*, like a source; arguably exempt) and `assets/meetings/2026-06-01-ceo-meeting/README.md` (missing `updated:`). Both under `assets/`; low priority.
- **8 NO-FRONTMATTER:** 4 exempt (log/index/AGENTS/CLAUDE), 2 presentation decks (outbound deliverables, borderline-exempt), 1 process spec (`standup-skill-v3.14-wiki-inbox-mirror-spec` — should get frontmatter), and the stray root file below.

### 7. Question aging → brief
No candidates. Zero active questions created before 2026-05-06; the active set is all recent.

### 8. Unresolved escalations (status:active AND updated >14d)
**1** — `2026-05-11-bonaventure-friday-meeting-audio-recovery` (updated 2026-05-11). This is carry-forward #8, externally blocked (needs MP3 + Whisper rerun). Not lint-fixable.
**Index drift fixed:** the index "Questions (open)" section listed **9** entries but only **4** are `status: active` in frontmatter. Moved the 5 resolved (`per-instance-curator-role` [ratified by v0.15 today], `claude-md-v0.14-schema-bump-proposal`, `yc-partner-entity-pages`, `marketing-stack-vendor-pages`, `vivian-balakrishnan-and-factset`) into the resolved section. Index now matches frontmatter.

### 9. Attribution discipline
**146 decision pages have `decided_by:` = a person with no `attribution:` field.** Per §4 the rule is **backfill on-touch, not retroactive** — so this is a *known backlog*, not 146 fixes. No backfill performed (correct per rule). These are pre-v0.14 HR/ISO/registry decisions (2026-04-20 → 2026-05-04 clusters). They'll acquire `attribution:` as they're touched. Recorded here as the standing backlog size so the next lint can track whether on-touch backfill is shrinking it.

## Structural findings (new since 2026-06-02)

1. **`people/jehad-altoutou/` nested corpus (1,116 files)** — Headline. Escalated → [[lint-2026-06-05-jehad-altoutou-nested-corpus]].
2. **Second marketing handoff bundle** — `marketing-handoff-2026-05-22-broader-marketing-corpus/` (67 files), sibling to the Singapore-monitoring bundle. Both are federation packages destined for the Marketing instance; they should eventually leave the AIO repo (carry-forward #7 territory).
3. **Stray empty root file** `2026-05-31.md` (0 bytes) — almost certainly an accidental/aborted daily-note create (possibly from `scripts/build-daily-snapshot.py`). Flagged, not deleted (didn't create it; want curator confirmation it's not an expected snapshot stub).
4. **`scripts/` directory now present** — `build-daily-snapshot.py` (dashboard snapshot builder) + `io.janusd.git-lock-cleanup.plist` (launchd job, presumably the index.lock mitigation). Not previously noted in any lint; benign, worth a one-line mention in §2 of CLAUDE.md eventually.

## Inline fixes applied this lint
- Removed `vendors/claude.md.tmp` + `vendors/google-cloud.md.tmp` (carry-forward #4, finally clearable from in-repo Claude Code).
- Fixed 2 broken `related:` slugs (check 5).
- Reconciled index "Questions (open)" ↔ frontmatter (moved 5 resolved out).

## Escalation filed
- [[lint-2026-06-05-jehad-altoutou-nested-corpus]] — the nested-corpus decision.

## Carry-forward queue for the next lint

1. **DECIDE the `people/jehad-altoutou/` nested corpus** ([[lint-2026-06-05-jehad-altoutou-nested-corpus]]). Highest priority — until resolved, every vault-wide count is distorted and the next lint must keep manually excluding it. Likely answer: it's a separate instance and belongs in its own repo (it literally has its own CLAUDE.md).
2. **Vendor junk cleanup** (curation): merge/delete `vendors/attio-duplicate-air-76.md`; delete `vendors/import-your-data-linear-placeholder.md`.
3. **8 `entities/vendors/` + 7 `entities/people/` stragglers** — one curation pass to migrate/reconcile (rolls up #2). Carried from 2026-06-02 lint items 2–3.
4. **CLAUDE.md §4 micro-clarification: vendor `confidence:`** — exempt AIR-mirrored vendor pages or auto-default, to clear the 60-page systematic gap. Candidate v0.16 one-liner.
5. **Missing-page triage** (check 4): confirm `llm-wiki-build`→`janus-prime-radiant-build` and `janus-html-deck`→`janus-html-deck-brand-guideline` stale-slug rewrites (safe once confirmed); decide whether to stub the 5 genuinely-missing meeting/page targets.
6. **Stray `2026-05-31.md`** — confirm it's not an expected `build-daily-snapshot.py` artefact, then delete.
7. **Second marketing-handoff bundle** — fold into the marketing-handoff resolution (carry-forward #7 from prior lints): both bundles should exit the AIO repo once the Marketing instance ingests them.
8. **Dimon duplicate** (curator-blocked) + **Fireflies-blocked bonaventure meeting** (external-blocked) — unchanged.
9. **Attribution backlog watch** — 146 person-attributed decisions lack `attribution:`; track on-touch backfill progress (do not retroactively sweep).

**Carry-forward intent:** items 1–3 are one curator/Jehad session (the corpus decision unblocks the cleanups). Items 4–7 are micro-tasks. 8–9 are standing.

## Notes for the curator
Mechanically the wiki is healthy — the only thing standing between this and a near-zero-finding lint is the `jehad-altoutou` corpus, which isn't an AIO-wiki problem so much as a *two-instances-in-one-repo* problem. Decide that one and the next lint is trivial. The 146-attribution backlog looks alarming but is rule-compliant (on-touch backfill); don't be tempted to mass-backfill — it would manufacture false-confidence attributions, exactly what §5.1 guards against.

Ingest counter since this lint: **0**.
