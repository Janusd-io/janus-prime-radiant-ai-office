---
type: pulse
title: Lint pass — first health check on the live Shared Drive vault (post-relocation)
slug: 2026-05-12-lint
created: 2026-05-12
updated: 2026-05-12
departments: [ai-office]
confidence: high
related: [janus-prime-radiant-build, 2026-05-07-lint-evening, 2026-05-07-lint, 2026-05-06-lint]
---

# Lint pass — 2026-05-12 (live Shared Drive vault)

First lint pass run against the live Shared Drive vault (`Janus Prime Radiant — AI Office`) after the 2026-05-08 relocation. A separate lint had run on the legacy `My Drive/Sandbox/LLM Wiki/` vault on 2026-05-08 — that vault is now frozen and its lint output (`pulse/2026-05-08-lint.md` over there) does *not* reflect the live state. This lint replaces it as the canonical current-state record.

**Wiki state at lint time:** 125 wiki pages (20 vendors, 0 clients, 2 external people, 10 internal, 9 department entities + 1 mesh subfolder with 3 files, 13 concepts, 3 processes, 9 projects, 24 decisions, 8 lessons, 11 questions [3 active, 8 resolved], 10 pulse entries [including this one], 4 briefs) · 79 source files (44 articles, 2 meetings, 4 Linear, 15 Notion, 14 Monday).

Vault root carries 5 files outside the wiki schema: `CLAUDE.md` (v0.9, 2026-05-11), `index.md`, `log.md`, `apply-standup-methodology-to-andrew-work-stream.md` (0 bytes, placeholder), `standup-skill-v3.14-wiki-inbox-mirror-spec.md`.

## Summary

Wiki is in excellent structural health under the live-vault microscope. Frontmatter compliance is 100% across all 125 pages. Zero true orphans (every page has either inbound references or a populated `related:` field). Zero stale-claims (every page whose body cites sources has an `updated:` field at or after the most recent cited source). The broken-wikilink set is small (16 distinct targets) and dominated by the same persistent intentional-deferral set the prior three lints flagged, with a small number of new findings from the 2026-05-08 → 2026-05-12 schema/rename churn.

**Three inline fixes made this pass** (all surfaced for the first time on the live vault):

- `decisions/2026-05-06-notion-role-shift-journal-not-knowledge-base.md` carried a stale `[[llm-wiki-build]]` wikilink. The 2026-05-08 rename sweep that renamed the project hub `llm-wiki-build` → `janus-prime-radiant-build` updated 9 files but missed this one. Rewrote to `[[janus-prime-radiant-build]]` with a parenthetical noting the rename. `updated:` bumped 2026-05-07 → 2026-05-12.
- `questions/ingest-2026-05-12-1530-mnemon.md` (the escalation written earlier today) carried two body wikilinks that were really meta-references, not wiki-page links: `[[CLAUDE.md]]` (the schema doc, sits at vault root, not a wiki page) and `[[2026-05-07 20:00]]` (a log-entry timestamp). Both rewritten to backtick form per the wiki's prose convention.

## Detailed findings

### 1. Contradictions

None surfaced.

### 2. Stale claims (page `updated:` < latest cited source)

Zero. Programmatically checked each page's `updated:` field against the most recent `fetched:` / `created:` date of every slug in its `sources:` list. All `updated:` fields are at or ahead of the latest source. The post-relocation schema churn (CLAUDE.md v0.6 → v0.9 in 4 days) drove many `updated:` bumps along the way, which kept this clean.

### 3. Orphan pages

**0 true orphans** (no inbound refs AND no `related:`). The schema discipline of populating `related:` on every page is holding.

**19 pages with 0 inbound wikilinks** (but `related:` populated):

*Expected — no action:*
- 9 resolved `questions/` pages (transient escalations)
- 1 lint pulse entry (`2026-05-07-lint-evening` — lint reports are orphans by design)
- 1 stub department entity (`engineering` — vocabulary placeholder per CLAUDE.md v0.8 dept-pages pass)
- 1 fresh active escalation (`ingest-2026-05-12-1530-mnemon` — just created today; back-links will emerge when Michael resolves)

*Carried-over back-link candidates (same set as 2026-05-07-evening and 2026-05-08-legacy lints; still not done — ~15-min editing pass closes them):*
- `lessons/2026-04-20-fireflies-summaries-insufficient-raw-transcript-canonical` → back-link from `entities/vendors/fireflies`
- `lessons/2026-04-20-gcp-self-host-metering-complexity-hostinger-simplicity-wins` → from `entities/vendors/google-cloud` and `entities/vendors/hostinger`
- `decisions/2026-05-06-backlog-cleanup-no-return-to-backlog` → from `entities/vendors/linear` and `processes/ai-tool-evaluation`
- `decisions/2026-05-06-skills-stay-as-skills-not-plugins` → from `entities/vendors/claude` and `concepts/agent-skills`
- `decisions/2026-05-06-standup-skill-v3-12-self-correcting-behavior` → from `processes/standup`

*New since 2026-05-08 legacy lint (back-link candidates from the 2026-05-11 standup batch):*
- `briefs/2026-05-11-andrew-onboarding-plan` → from `entities/internal/andrew-soane` and `projects/marketing-prime-radiant`
- `questions/2026-05-11-bonaventure-friday-meeting-audio-recovery` → from `entities/internal/bonaventure-wong`
- `questions/2026-05-11-internal-branding-prime-radiant-vs-nomi-vs-brain` → from `projects/janus-prime-radiant-build`
- `projects/april-2026-aio-transcripts-recovery` → from `processes/standup` and `entities/vendors/fireflies`

### 4. Missing pages (multi-reference concepts/entities without a page)

No fresh missing-pages clusters this pass. The hyperproductivity cluster flagged in the 2026-05-07-evening lint hasn't accumulated additional sources; still on watch but doesn't yet warrant promotion. The Turbopuffer reference (in `concepts/retrieval-augmented-generation` and `briefs/post-rag-agent-data-stack`) remains a single-mention pair; not yet promotion-worthy.

### 5. Broken references

**16 distinct broken wikilink targets** in non-lint pages (down from 23 raw, after removing self-references inside historical lint reports + the three inline-fixed items above). Categorised:

| Target | Refs | Category | Action |
|---|---|---|---|
| `[[n8n]]` | 2 | Persistent — deferred vendor (Mivory escalation) | None; auto-resolves on promotion |
| `[[ai-registry-v2]]` | 2 | Persistent — Monday tracking item, not a project hub | None |
| `[[ai-registry]]` | 2 | Slash-command reference in Jehad's federated briefs (`aio-skills-sor-architecture-jehad`, `aio-playbooks-jehad`) | None; federated content, coordinate with Jehad on prose convention |
| `[[knowledge-compilation]]` | 1 | Persistent — deferred concept (awaiting 2nd source) | None |
| `[[notion-operations-notebook-restructure]]` | 1 | Persistent — Monday tracking item | None |
| `[[build-categorisation-taxonomy-for-ai-tools]]` | 1 | Persistent — Monday tracking item | None |
| `[[openai]]` | 1 | Pending promotion candidate (flagged 2026-05-08 legacy lint; now reinforced by Mnemon ingest also touching `agent-memory`) | **Promote — see recommendations** |
| `[[notebooklm]]` | 1 | Intentional — retired vendor per 2026-05-11 decision; not promoting | None |
| `[[github]]` | 1 | Deferred — flagged in 2026-05-11 batch log as "appropriate eventually given GitHub's now-confirmed backbone role, but defer to a focused pass" | None this pass |
| `[[marketo]]` | 1 | Deferred — flagged in 2026-05-08 Andrew transcript ingest as escalation-deferred | None this pass |
| `[[janus-prime-radiant-template]]` | 1 | External — GitHub template repo (Janusd-io org), not a wiki entity. | Consider rewriting `projects/janus-prime-radiant-build.md` reference to a URL or backtick form |
| `[[apply-standup-methodology-to-andrew-work-stream]]` | 1 | Vault-root placeholder — 0-byte file exists at vault root; technically resolves in Obsidian but file is empty | None — flagged 2026-05-11 batch log as known empty placeholder |
| `[[2026-04-30-aio-ito-compliance]]` | 1 | Missing source reference in `entities/departments/it-ops.md` — looks like a Linear/Notion item not yet captured as a source | Investigate: does this refer to an actual document or an aspirational placeholder? |

Two additional targets resolve in Obsidian via vault-root files but are *not* wiki pages — left as-is because the wiki convention is to use backticks for non-schema files:

- The three inline-fixed wikilinks (above) are out of this set now.

### 6. Frontmatter compliance

**100% — zero issues across 125 pages.** Programmatic check: required base fields (`type`, `title`, `slug`, `created`, `updated`), type-specific required fields (`confidence` on vendor/pulse/brief; `owner`+`status` on project/decision/question; `status` on lesson), and slug-vs-filename match — all clean.

The 2026-05-08 legacy lint's two YAML-bracket findings (April-era decisions with `[[wikilink]]` syntax inside `related:`) are not present on the live vault — those files were either fixed in the legacy vault before relocation, or the relocation copied a cleaner version. Final-pass bracket grep confirms zero residue across all `related:` fields wiki-wide.

### 7. Question aging

No open questions older than 30 days. The wiki's three active questions are all from 2026-05-11 (1 day old) or 2026-05-12 (today). No promotion-to-brief candidates this pass.

### 8. Unresolved ingest escalations

Two unresolved `ingest-*` escalations, both well under the 14-day flag threshold:

- `ingest-2026-05-11-standup-skill-dual-write-to-aio-inbox` (1 day old, status `active`) — Jehad's `/standup` v3.14 dual-write proposal; provisionally agreed; routing/order/failure-mode questions awaiting Michael
- `ingest-2026-05-12-1530-mnemon` (today, status `active`) — proposed Mnemon vendor page; recommendation = defer per single-source rule

## Source quality issues

No new finding this pass. Earlier Web Clipper date-parsing bug observation (Anthropic blog clips with `published: 2001-XX-XX` Y2K mis-parse) carries forward — those source files retain incorrect frontmatter per source-immutability, no new affected clips since 2026-05-07.

## What's working well

- **Schema discipline survived four CLAUDE.md version bumps in four days** (v0.6 → v0.7 → v0.8 → v0.9). Each schema change correctly propagated to entity pages, index, and dependent rules. The 2026-05-08 mass rename (`llm-wiki-build` → `janus-prime-radiant-build`) propagated cleanly across 9 files in one sweep — only one straggler found here (now fixed).
- **The 2026-05-08 batch (Andrew brainstorm + Tier 1/Tier 2 + 8 department pages + v0.8 schema) introduced zero broken refs and zero frontmatter violations.** Subagent-led ingest discipline is holding at scale.
- **The 2026-05-11 standup batch (1 source + 12 new wiki pages + 10 existing-page patches) also introduced zero broken refs.** The mesh-federation pattern landed without breaking anything.
- **Federation discipline:** the `entities/departments/marketing/` mesh subfolder (3 files including README) coexists with the flat `entities/departments/<slug>.md` entity pages without slug collisions or graph confusion. The first cross-vault structural change worked.
- **Lint cadence behaviour:** the legacy-vault lint running against the frozen copy on 2026-05-12 morning would have been confusing if not caught — calling this out as a precedent: when the active mount path is ambiguous, run a quick *proof-of-mount* (e.g., `tail log.md`, check `CLAUDE.md` version) before lint to confirm which instance is being checked.

## Recommendations (priority order)

1. **Promote `openai` to a vendor page** via a `questions/ingest-*.md` escalation. Two sources now back the reference (`openai-agents-sdk-session-memory` and the dual-position framing in [[agent-memory-2026-q2]] reinforced by the Mnemon ingest). This was flagged in the 2026-05-08 legacy lint and has been hedged ever since; the second-source threshold is met.
2. **Run the back-link sweep** (5 carried-over orphans + 4 new orphans from the 2026-05-11 batch = 9 total). ~20 minutes; closes the orphan list, makes the dashboard's cross-link-density signal honest.
3. **Investigate `[[2026-04-30-aio-ito-compliance]]`** referenced from `entities/departments/it-ops.md`. Either capture the source it refers to (and convert to a backtick source-slug citation), or rewrite the body to remove the broken wikilink if it was aspirational.
4. **Consider rewriting `[[janus-prime-radiant-template]]` in `projects/janus-prime-radiant-build.md`** to a URL or backtick form — the template is a GitHub repo, not a wiki entity, and the wikilink will never resolve. Mechanical fix; do whenever the project hub is next touched.
5. **Coordinate with Jehad on `[[/ai-registry]]` slash-command references** in the two federated personal-vault briefs (`aio-skills-sor-architecture-jehad`, `aio-playbooks-jehad`). Wiki convention is backticks for slash commands; these are his prose framing, so worth a quick alignment before silent-editing federated content.
6. **CLAUDE.md template repo at v0.8.0** — the `janus-prime-radiant-template` GitHub repo still carries CLAUDE.md v0.8.0 even though the AIO vault is at v0.9 (ISO added to department vocabulary). Already noted as queued on `projects/janus-prime-radiant-build.md`. Bump to v0.9.0 template release when next touching the repo.
7. **Recurring infra:** the empty `apply-standup-methodology-to-andrew-work-stream.md` (0 bytes) at vault root should either be populated or deleted. Currently just creates confusion for any future grep.

## Reset

Ingest counter resets to 0 at this lint entry. Ingests since last lint (2026-05-07 19:00): 3 (OpenAI Cookbook 2026-05-07 20:00 + 2026-05-11 standup batch + 2026-05-12 mnemon). Next automatic lint trigger fires at ingest count 10.
