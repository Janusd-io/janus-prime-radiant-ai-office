---
type: question
title: "Create a concept page for GEO (Generative Engine Optimization)?"
slug: ingest-2026-06-04-1030-generative-engine-optimization-concept
created: 2026-06-04
updated: 2026-06-09
departments: [ai-office, marketing]
status: active
owner: michael-bruck
sources: [googles-ai-shift-collective-freak-out, google-search-as-you-know-it-is-over]
related: [2026-06-03-google-search-ai-platform-geo-shift, agentic-lean-marketing-stack]
---

# Create a concept page for GEO (Generative Engine Optimization)?

**Proposed action.** Create `concepts/generative-engine-optimization.md` capturing GEO — the emerging discovery-optimisation discipline that replaces SEO/PageRank as AI answers displace blue links.

**Why escalated (per §5.1 trust line).** Creating a new concept page is high-stakes, and here there's a specific **slug-collision risk**: "geo" already appears in the wiki in its *geographic* sense (geo-routing / hreflang in [[agentic-lean-marketing-stack]]). Using the bare slug `geo` would be ambiguous; the proposal is the unambiguous slug `generative-engine-optimization` with `geo` reserved as a display alias only where context is clear.

**Substance to capture (from [[googles-ai-shift-collective-freak-out]]).**
- GEO = optimising to be *cited by AI answers* rather than *ranked in blue links*. "Generative" replaces "search" as the first word.
- Mechanism shift: from backlinks/keyword-spamming (PageRank signals) → seeding positive *conversations* across published articles, Trustpilot reviews, and forums (Reddit especially, a favoured AI-scraping source). More human-flavoured, reportedly harder to game than star-rating systems.
- AI models answer product queries by analysing aggregate sentiment across those sources.
- WPP's Daniel Hulme framing: *"Humans tend to buy using emotion, but AI agents will be more rational."*

**Why it's worth a durable page.** GEO is directly load-bearing for the Marketing Prime Radiant instance (it's a core operating-principle shift for that domain) and is referenced from the new pulse [[2026-06-03-google-search-ai-platform-geo-shift]]. It will recur.

**Alternatives considered.**
1. Fold GEO into the existing [[agentic-lean-marketing-stack]] brief — rejected: GEO is a concept/methodology, not a stack component, and is reused beyond that brief.
2. Leave it in the pulse only — rejected: pulse is the timeline, not the atemporal current view; GEO will be referenced repeatedly.
3. Create now without escalation — rejected per trust line (new concept + slug collision).

**Recommendation.** Approve `concepts/generative-engine-optimization.md`. Confirm the slug (vs e.g. `geo-generative-engine-optimization`) and the home instance (AIO vs Marketing — likely Marketing-canonical with an AIO stub, once the Marketing instance has a concepts surface).

---

## Status (2026-06-09) — still open, pending Michael's call

**Not yet actioned.** No `concepts/generative-engine-optimization.md` exists, and no June meeting/decision records an approval. The supporting pulse [[2026-06-03-google-search-ai-platform-geo-shift]] is filed; the recommendation (approve the page under the unambiguous slug `generative-engine-optimization`) still stands.

**Still needs Michael's decision on two points** before the page is created: (1) confirm slug `generative-engine-optimization` (vs `geo-…`); (2) home instance — AIO vs Marketing-canonical (likely Marketing once that instance has a concepts surface). Held per the §5.1 trust line (new concept page = high-stakes). Owner: [[michael-bruck|Michael]]. Stays `active`.
